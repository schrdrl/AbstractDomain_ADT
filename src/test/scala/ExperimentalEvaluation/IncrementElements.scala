package ExperimentalEvaluation
import org.scalatest.funsuite.AnyFunSuite

class IncrementElements extends AnyFunSuite {
  /**
   * While an AList value xs is not ANil repeat:
   * - assign its tail as its new value
   * - add one to the counter n : AInt
   *
   * assert xs == ANil
   * assert n  >= 0 (length is positive)
   */

}
